openapi: 3.1.0
info:
  title: Eino CLI Control Plane
  version: 0.1.0
  description: >-
    MVP control-plane contract for the local Eino CLI. These endpoints describe
    logical module boundaries for REPL, orchestrator, tools, session recovery,
    and task tracking; they do not imply a remote network deployment.
servers:
  - url: local://eino-cli
paths:
  /sessions:
    post:
      summary: Start a new CLI session
      operationId: createSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSessionRequest'
      responses:
        '201':
          description: Session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
  /sessions/{sessionId}/commands:
    post:
      summary: Submit a natural-language input or slash command
      operationId: submitCommand
      parameters:
        - $ref: '#/components/parameters/SessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitCommandRequest'
      responses:
        '202':
          description: Command accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommandAccepted'
  /agent-runs/{runId}:
    get:
      summary: Get the current single-agent run state
      operationId: getAgentRun
      parameters:
        - $ref: '#/components/parameters/RunId'
      responses:
        '200':
          description: Agent run state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRun'
  /tool-invocations/{invocationId}/approve:
    post:
      summary: Approve or reject a pending high-risk tool invocation
      operationId: approveToolInvocation
      parameters:
        - $ref: '#/components/parameters/InvocationId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecision'
      responses:
        '200':
          description: Approval decision recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolInvocation'
  /sessions/{sessionId}/checkpoint:
    post:
      summary: Persist the latest recoverable checkpoint for a session
      operationId: saveCheckpoint
      parameters:
        - $ref: '#/components/parameters/SessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveCheckpointRequest'
      responses:
        '200':
          description: Checkpoint saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Checkpoint'
  /sessions/{sessionId}/resume:
    post:
      summary: Resume the latest checkpoint for a session
      operationId: resumeSession
      parameters:
        - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: Session resumed from latest checkpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResumeSessionResponse'
  /sessions/{sessionId}/tasks:
    get:
      summary: List task state for a session
      operationId: listTasks
      parameters:
        - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: Current task list
          content:
            application/json:
              schema:
                type: object
                properties:
                  tasks:
                    type: array
                    items:
                      $ref: '#/components/schemas/Task'
                required: [tasks]
components:
  parameters:
    SessionId:
      name: sessionId
      in: path
      required: true
      schema:
        type: string
    RunId:
      name: runId
      in: path
      required: true
      schema:
        type: string
    InvocationId:
      name: invocationId
      in: path
      required: true
      schema:
        type: string
  schemas:
    CreateSessionRequest:
      type: object
      properties:
        workspaceRoot:
          type: string
      required: [workspaceRoot]
    SubmitCommandRequest:
      type: object
      properties:
        rawInput:
          type: string
        inputType:
          type: string
          enum: [natural_language, slash_command]
      required: [rawInput, inputType]
    CommandAccepted:
      type: object
      properties:
        command:
          $ref: '#/components/schemas/Command'
        run:
          $ref: '#/components/schemas/AgentRun'
      required: [command, run]
    ApprovalDecision:
      type: object
      properties:
        decision:
          type: string
          enum: [approved, rejected]
        reason:
          type: string
      required: [decision]
    SaveCheckpointRequest:
      type: object
      properties:
        sessionId:
          type: string
        workspaceRoot:
          type: string
        lastInput:
          type: string
        awaitingApproval:
          type: boolean
      required: [sessionId, workspaceRoot, awaitingApproval]
    Session:
      type: object
      properties:
        id:
          type: string
        workspaceRoot:
          type: string
        startedAt:
          type: string
          format: date-time
        lastActiveAt:
          type: string
          format: date-time
      required: [id, workspaceRoot, startedAt, lastActiveAt]
    Command:
      type: object
      properties:
        id:
          type: string
        sessionId:
          type: string
        rawInput:
          type: string
        inputType:
          type: string
          enum: [natural_language, slash_command]
        status:
          type: string
          enum: [accepted, running, completed, failed]
        output:
          type: string
        errorCode:
          type: string
        errorMessage:
          type: string
        createdAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
      required: [id, sessionId, rawInput, inputType, status, createdAt]
    AgentRun:
      type: object
      properties:
        id:
          type: string
        sessionId:
          type: string
        commandId:
          type: string
        modelName:
          type: string
        status:
          type: string
          enum: [pending, streaming, completed, failed]
        result:
          $ref: '#/components/schemas/Result'
        startedAt:
          type: string
          format: date-time
        invocations:
          type: array
          items:
            $ref: '#/components/schemas/ToolInvocation'
      required: [id, sessionId, commandId, modelName, status, startedAt]
    Result:
      type: object
      properties:
        success:
          type: boolean
        code:
          type: string
          enum: [config_error, workspace_error, runtime_error, tool_error]
        message:
          type: string
        output:
          type: string
        needsUser:
          type: boolean
      required: [success]
    ToolInvocation:
      type: object
      properties:
        id:
          type: string
        toolName:
          type: string
        arguments:
          type: array
          items:
            type: string
        approvalStatus:
          type: string
          enum: [not_required, awaiting_approval, approved, rejected]
        executionStatus:
          type: string
          enum: [requested, executing, succeeded, failed, rejected]
        output:
          type: string
        errorMessage:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, toolName, arguments, approvalStatus, executionStatus, createdAt]
    Checkpoint:
      type: object
      properties:
        sessionId:
          type: string
        workspaceRoot:
          type: string
        lastInput:
          type: string
        awaitingApproval:
          type: boolean
        updatedAt:
          type: string
          format: date-time
      required: [sessionId, workspaceRoot, awaitingApproval, updatedAt]
    ResumeSessionResponse:
      type: object
      properties:
        session:
          $ref: '#/components/schemas/Session'
        checkpoint:
          $ref: '#/components/schemas/Checkpoint'
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/Task'
      required: [session, checkpoint, tasks]
    Task:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [pending, in_progress, completed, blocked, failed]
      required: [id, title, status]
